/bin
*.test
.envrc
.DS_Store
/coverage*

# Block a top-level binary accidentally produced by `go build -o schemalock`.
/schemalock

# Per-developer agent context. Everything under .claude/ is local-only.
.claude/

# JetBrains IDE project files (per-developer, local-only).
.idea/
